declare global {
  interface Window {
    cache: Map<string, unknown>;
  }

  namespace App {
    // interface Error {}
    // interface Locals {}
    // interface PageData {}
    // interface Platform {}
  }
}

export { };
